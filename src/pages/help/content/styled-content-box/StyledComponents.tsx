import { Box, styled, Typography } from "@mui/material";

export const StyledContentBox = styled(Box)(() => ({
  marginLeft: "10%",
  marginRight: "10%",
}));

export const CustomStyledTypographyHeader = styled(Typography)(() => ({
  color: "#DAA520",
  fontWeight: "bold",
}));
